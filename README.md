# 🔐 加密解密工具 / Encrypt-Decrypt Tool — V0.3.4

> 一个功能全面、界面精美的本地加密解密 Web 工具，支持多种文本编码方式、图片与 Base64 互转，音频与 Base64 互转，以及通用文件转换。所有操作均在浏览器本地完成，无需服务器，保护隐私安全。  
> 版本 0.3.4 全面重写了界面样式，采用现代蓝色主题、移除冗余渐变、优化毛玻璃效果，并统一设计变量，整体视觉更简洁专业。

---

## ✨ 功能特性

### 文本加密 / 解密
- **Base64** 编码/解码  
- **URL** 编码/解码  
- **HTML 实体** 编码/解码  
- **十六进制 (Hex)** 与文本互转  
- **二进制** 与文本互转  

### 图片处理
- 图片 → Base64：将图片转换为 DataURL 格式的 Base64 字符串  
- Base64 → 图片：将 Base64 字符串还原为图片并支持下载  

### 音频处理
- 音频 → Base64：支持 MP3、WAV、OGG 等格式转 Base64  
- Base64 → 音频：将 Base64 还原为音频并支持在线播放、下载  

### 通用文件转换
- 文件 → Base64：支持 PDF、ZIP、TXT 等任意文件转换为 Base64  
- Base64 → 文件：将 Base64 还原为文件并支持自定义文件名下载  

### 哈希与加密
- **MD5 / SHA-1 / SHA-256 / SHA-512**：为文本生成常见哈希摘要  
- **AES-256-GCM 加密/解密**：使用 WebCrypto API 实现真正的对称加密，需输入密码  

### 界面与体验
- 🌓 **深色/浅色主题** 一键切换（支持跟随系统）  
- 💡 输入/输出区域支持清空、一键复制  
- 📱 响应式设计，完美适配移动端  
- 🚀 纯本地运行，无服务器依赖，隐私安全  

---

## 🚀 快速开始

### 方法一：使用 Python 启动本地服务器（推荐）
项目内置了快速启动脚本，会自动打开浏览器。

```bash
# 确保已安装 Python 3
# Windows 用户双击 `启动.bat` 即可
# 或在终端执行：
python 快速启动.py
```

### 方法二：直接打开 HTML 文件
由于浏览器安全限制（如跨域、剪贴板 API 限制），部分功能可能无法在 `file://` 协议下正常工作，建议通过本地服务器运行。

---

## 🎨 界面与性能（V0.3.3 更新）

从 V0.3.3 开始，**项目新增了多项实用功能**：

- 新增 **MD5 / SHA-1 / SHA-256 / SHA-512** 哈希计算工具  
- 新增 **AES-256-GCM** 对称加密/解密功能，支持密码保护  
- 新增**通用文件转换**模块：支持任意文件与 Base64 互转  
- 优化文本工具 UI：选择哈希时隐藏解密按钮，选择 AES 时显示密码输入框  

V0.3.2 已完成的安全加固与代码清理：

- 清理了废弃的样式文件，统一 CSS 文件管理  
- 移除了不必要的 FPS 计数器（默认隐藏，Ctrl+Shift+F 可唤起）  
- 移除了未实现的"语言切换"按钮，避免功能承诺未兑现  
- 修复 `htmlDecode` 函数的 XSS 安全漏洞，改用更安全的解码方式  
- 精简 CSS 冗余代码，使用 CSS 变量统一管理样式  
- 优化启动脚本端口处理，增加端口占用自动检测  
- 修复摩尔斯电码空输入异常、事件监听重叠等问题  

---

## 🛠️ 技术栈

- 纯原生 **HTML5 / CSS3 / JavaScript（ES6+）**  
- 无任何第三方依赖  
- 使用 **FileReader、TextEncoder / TextDecoder、Clipboard API** 等现代 Web API  
- 启动脚本使用 Python 内置 HTTP 服务器  

---

## 📁 项目结构

```
.
├── index.html          # 主页面
├── style.css           # 样式文件（V0.3.4 全面重写，现代蓝色主题、统一变量）
├── script.js           # 主逻辑（文本/图片/音频/文件转换、哈希、AES 加密）
├── 快速启动.py         # Python 启动脚本（V0.3.2 增加端口自动检测）
├── 启动.bat            # Windows 批处理启动文件
└── README.md           # 说明文档（当前为 V0.3.4）
```

---

## 📋 更新日志

### V0.3.4 (2026-08-04)
- 🎨 **全面重写界面样式**：采用现代蓝色主题，视觉更简洁专业
- 🚫 **移除冗余渐变**：背景与组件统一使用纯色，降低视觉干扰
- ✨ **优化毛玻璃效果**：保留核心元素质感，避免过度装饰
- 🧹 **统一设计变量**：使用 `--primary`、`--bg`、`--surface` 等语义化变量，便于维护
- 📝 **更新项目文档与版本标识至 V0.3.4**

### V0.3.3 (2026-08-04)
- ✨ **新增哈希工具**：支持 MD5、SHA-1、SHA-256、SHA-512 文本哈希计算
- ✨ **新增 AES-256-GCM 加密/解密**：使用 WebCrypto API，支持密码保护
- ✨ **新增通用文件转换**：支持任意文件（PDF、ZIP、TXT 等）与 Base64 互转
- 🎨 **优化文本工具 UI**：选择哈希时隐藏解密按钮，选择 AES 时显示密码输入框
- 📝 **更新项目文档至 V0.3.3**

### V0.3.2 (2026-06-29)
- 🧹 **清理冗余文件**：删除废弃的 `style.css`，精简 `style-no-effects.css` 并重命名为 `style.css`
- 🚫 **移除多余 FPS 计数器**：默认隐藏，按 `Ctrl+Shift+F` 可显示
- 🧹 **移除未实现的语言切换按钮**：避免功能承诺未兑现的体验问题
- 🔒 **修复 XSS 安全漏洞**：`htmlDecode` 改用更安全的 `textarea` 方案替代 `innerHTML`
- 🐛 **修复摩尔斯电码空输入异常**：增加空输入判断，避免抛出错误
- 🐛 **修复事件监听重叠**：图片/音频转换时正确重置 `onload`/`onerror` 事件
- 🎨 **精简 CSS 冗余**：合并重复样式 `.action-btn` / `.download-btn`，使用 CSS 变量统一管理过渡时间、圆角等
- 🔧 **优化启动脚本**：增加端口占用自动检测，不再因端口被占用而报错退出
- 📝 **更新项目文档至 V0.3.2**

### V0.3.1 (2026-03-28)
- 🎨 **统一视觉模式**：移除"有特效版"与"无特效版"，改为统一的"正常版"，简化体验  
- 🚫 移除首次访问时的性能选择弹窗  
- 🧹 移除右上角版本切换按钮，界面更简洁  
- ⚡ 优化页面加载速度，降低内存占用  
- 📝 更新项目文档至 V0.3.1  

### V0.3 (2026-02-18)
- ✨ 新增：音频处理模块（音频 ↔ Base64 互转）  
- ✨ 新增：音频预览与下载功能  
- ⚡ 优化：完善特效切换机制，减少页面重绘  
- 🐛 修复：某些情况下 Base64 解码的兼容性问题  

### V0.2 (2026-02-16)
- ✨ 新增：二进制编码/解码功能  
- ✨ 新增：特效版本切换机制（有特效 / 无特效）  
- ✨ 新增：实时 FPS 计数器（仅在有特效版显示）  
- ✨ 新增：主题跟随系统选项  
- ⚡ 优化：大幅降低无特效版的 CPU/GPU 占用  
- 🐛 修复：URL 解码时的异常处理  
- 📱 改进：移动端适配细节  

### V0.1 (初始版本)
- 基础文本编码/解码（Base64、URL、HTML 实体、十六进制）  
- 图片与 Base64 互转  
- 深色/浅色主题手动切换  
- 复制、清空等基础交互  

---

## ⚠️ 注意事项

- 所有数据均在本地处理，不会上传到任何服务器，请放心使用。  
- 解码时请注意输入格式正确，否则会提示错误。  
- 图片/音频转 Base64 生成的字符串较长，复制时请耐心等待。  
- 如需恢复旧版特效效果，可下载 V0.3 版本使用。  

---

## 🤝 贡献

欢迎提交 Issue 或 Pull Request 来改进这个项目！如果您有任何建议或发现了 Bug，请及时反馈。
